#include <bits/stdc++.h>
using namespace std;

int main()
{
    // Unordered Map is just like unordered set, random, also doesn't store duplicates.
    unordered_map<int, string> um;
    // upper_bound() and lower_bound() doesn't work here
    return 0;
}